from utils.constants import *



GRID_NAME_LIST = [
    PATH_112신고접수정보_AFTER.split('/')[-1].split('.')[0],
    PATH_CCTV_AFTER.split('/')[-1].split('.')[0],
    PATH_스마트보안등_AFTER.split('/')[-1].split('.')[0],
    PATH_여성안심택배함_AFTER.split('/')[-1].split('.')[0],
    PATH_여성안심지킴이집_AFTER.split('/')[-1].split('.')[0],
    PATH_아동안전지킴이시설물_AFTER.split('/')[-1].split('.')[0],
    PATH_편의점_AFTER.split('/')[-1].split('.')[0],
    PATH_노래연습장_AFTER.split('/')[-1].split('.')[0],
    PATH_단란주점_AFTER.split('/')[-1].split('.')[0],
    PATH_목욕장업_AFTER.split('/')[-1].split('.')[0],
    PATH_숙박업_AFTER.split('/')[-1].split('.')[0],
    PATH_유흥주점_AFTER.split('/')[-1].split('.')[0],
    PATH_인터넷컴퓨터게임시설_AFTER.split('/')[-1].split('.')[0],
    PATH_경찰서_AFTER.split('/')[-1].split('.')[0],
    PATH_치안센터_AFTER.split('/')[-1].split('.')[0],
    PATH_파출소지구대_AFTER.split('/')[-1].split('.')[0],
]


GRID_PATH_LIST = [PATH_112신고접수정보_AFTER, PATH_CCTV_AFTER, PATH_스마트보안등_AFTER, PATH_여성안심택배함_AFTER, PATH_여성안심지킴이집_AFTER,
                 PATH_아동안전지킴이시설물_AFTER, PATH_편의점_AFTER, PATH_노래연습장_AFTER, PATH_단란주점_AFTER, PATH_목욕장업_AFTER,
                 PATH_숙박업_AFTER, PATH_유흥주점_AFTER, PATH_인터넷컴퓨터게임시설_AFTER, PATH_경찰서_AFTER, PATH_치안센터_AFTER,
                 PATH_파출소지구대_AFTER]